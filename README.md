# F1-data-dasboard
A dashboard where the user selects the driver(s) or team(s), year, grand prix and metric. Afterwards, the user selects how the graph is presented (for example, standard rolling deviation recorded every 5 laps). The program then generates a graph using the selected information.
